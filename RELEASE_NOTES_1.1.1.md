# HyperChanger v1.1.1

**HyperChanger 1.1.1 正式版发布！**

这是一个面向部分小米 HyperOS 4 Beta 系统的 LSPosed 自定义模块。本版本以修复 Bug、提升不同 HyperOS 构建兼容性和 System UI 重建后的稳定性为主，并补充通知与媒体通知调试工具及若干设置扩展。

**新增**

* `Feature` 新增通知模拟器，可发送普通通知、焦点通知以及媒体通知，普通通知支持批量发送、延迟发送和自定义通知内容/图片。
* `Feature` 锁屏小组件支持单独设置背景材质，并完善天气、电量、步数、闹钟和日程信息的组合显示。
* `Feature` 系统设置支持扩展自动锁屏时长选项和 Google 服务入口；状态栏信号与网络类型提供更细的调整选项。
* `Feature` 超级小爱键盘新增高光、阴影、描边和底部高光等玻璃外观参数。
* `Feature` 支持去除媒体超级岛下拉条的白名单限制。

**变更与优化**

* `UI/UX` 优化柔光玻璃下拉菜单、弹窗、工具栏和滑块交互，滑块支持直接输入数值和恢复默认值。
* `UI/UX` 完善语言包导入、编辑、删除和系统语言识别，补齐中英文/日文内置资源与示例语言包。
* `Optimize` 优化锁屏小组件在通知避让、深度时钟、多行闹钟文本和步数宽布局下的定位与尺寸处理。
* `Optimize` 改进 System UI、设置外观和超级小爱 Hook 的反射查找、异常保护与配置迁移，降低系统重建或字段差异导致的功能失效。
* `Optimize` 优化状态栏双排信号和网络类型的兼容性处理，减少不同系统版本下的显示异常。

**修复与重构**

* `Fix` 修复较新 HyperOS 4 构建中隐藏移动网络类型选项处理不一致的问题，避免独立网络类型模式被错误隐藏。
* `Fix` 修复锁屏小组件步数宽布局无数据显示、闹钟日期文本挤压以及深度时钟多层布局偏移等问题。
* `Fix` 修复媒体通知、锁屏迷你播放器和玻璃材质在视图重建、圆角裁剪及参数变化时的显示异常。
* `Fix` 修复设置外观素材同步、相机/系统设置反射入口和超级小爱键盘旧版参数读取的兼容性边界情况。
* `Refactor` 重构部分配置读写和作用域 Hook 安装流程，统一默认值、迁移逻辑与保护性处理。

---

## 问题反馈须知

欢迎提交 Bug 和改进建议。涉及应用崩溃、异常报错、白屏或功能失效时，请附上完整的 **Logcat 日志**、设备型号、Android/HyperOS 版本、启用的作用域和复现步骤。

> 🚨 **NO LOG, NO FIX.**
>
> 无日志、无复现步骤且只有一句描述的 Issue 将不予受理并直接关闭。

感谢每一位参与测试和反馈的用户，让 HyperChanger 变得更好！

**APK SHA-256**

`0C6A5E445C04F3B429E6E63F79DA7B1AFF6E942E0175C92D4C8DF4DF6604715F`
